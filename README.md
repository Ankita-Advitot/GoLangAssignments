# 💰 Expense Tracker REST API (Go + PostgreSQL)

This project is a **RESTful Expense Tracker API** built using **Go (Golang)** following the **MVC (Model–View–Controller) architecture**.  
It allows users to record, view, and manage their expenses with persistent storage using **PostgreSQL**.

---

## 🧱 Architecture

This project follows a **layered MVC pattern**:

